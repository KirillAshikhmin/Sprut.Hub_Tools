# Спецификация: 💨 Автоматизация вытяжки по присутствию и влажности

Папка сценария: `ExhaustFanAutomation/`. Тип: логический сценарий (Nashorn, ES5 + точечный ES6).
Эталон: `MotionLightAutomation` — структура файла, стиль кода, состав блоков опций, формат README.

> Ревизия 2 — после независимой проверки покрытия (гейт G2). Что изменилось:
> убран `HS.Outlet`; §4 объясняет прочтение «контрольный +5 %» и момент пересчёта порога;
> §7 излагает семантику ручных входов целиком, а не ссылкой на эталон; §8 получил
> блокировку после предельного таймера; §10 перечисляет, что из эталона НЕ переносится.

## Задача

Вытяжка в санузле живёт в двух крайностях: либо её включают руками и забывают на полдня,
либо она висит на выключателе света и тарахтит впустую. После душа влажность держится ещё
долго после того, как человек вышел, — а таймер этого не знает. Зимой и в сырую погоду
«целевые 60 %» вообще недостижимы, и вытяжка молотит вхолостую до бесконечности.

## Решение

Один логический сценарий, который вешается на саму вытяжку (выключатель или вентилятор).
Он включает её, когда в комнате действительно кто-то задержался, продолжает работу, пока
не упадёт влажность, ориентируется на «сухую» комнату, чтобы не гнаться за недостижимой цифрой,
и в любом случае выключается не позже заданного предела. Ручное управление и «рубильник»
автоматики устроены ровно так же, как в эталонном сценарии света.

## Пользовательские истории

| # | Метка | История | Приёмка |
|---|-------|---------|---------|
| 1 | R01, R19 | Как владелец хаба, я получаю сценарий, устроенный как «Автоматизация света по движению», чтобы не переучиваться | те же блоки опций, тот же стиль README, тот же формат публикации |
| 2 | R02, R05 | Как владелец хаба, я привязываю сценарий к самой вытяжке, чтобы логика жила на устройстве | `sourceServices` = `Switch`, `FanBasic`, `Fan`; `sourceCharacteristics` = `On`, `Active` |
| 3 | R20i | Как пользователь, я выбираю до трёх датчиков активности, чтобы покрыть комнату | три слота `Датчик 1…3`: движение, присутствие, открытие |
| 4 | R03, R04 | Как пользователь, я хочу, чтобы вытяжка включалась не на каждый чих, а когда человек задержался | вытяжка включается, когда присутствие держится дольше `Задержка включения` (120 с по умолчанию) |
| 5 | R03.1 | …и чтобы импульсный PIR не обнулял отсчёт каждые 30 секунд | отсчёт сбрасывается, только когда активности нет дольше `Задержка выключения`; короткий провал отсчёт не сбрасывает |
| 6 | R03.2 | …и чтобы отсчёт не «висел» после ухода человека | активность пропала и прошла `Задержка выключения` → отсчёт начинается заново с нуля |
| 7 | R06 | Как пользователь, я хочу, чтобы вытяжка гасла через 5 минут после ухода | все датчики неактивны дольше `Задержка выключения` (300 с) → выключение |
| 8 | R08, R10 | Как пользователь, я могу не указывать датчик влажности, и тогда работает чистая логика по присутствию | поле пустое → выключение только по таймеру присутствия |
| 9 | R09 | Как пользователь, я хочу, чтобы после душа вытяжка досушила комнату | датчик влажности выбран → выключение только когда влажность ≤ рабочего порога **и** таймер присутствия истёк |
| 10 | R11, R12, R24i | Как пользователь, я хочу верхний предел работы на любой случай | `Максимальное время непрерывной работы` (180 мин) гасит вытяжку при любом включении: по присутствию, по влажности, вручную |
| 11 | R11.1 | …и чтобы после предела вытяжка не включилась обратно через секунду | после срабатывания предела авто-включение заблокировано, пока повод не исчезнет сам |
| 12 | R13, R14, R15 | Как пользователь, я указываю контрольный датчик в сухой комнате, чтобы не гнаться за недостижимой влажностью | контрольный ≥ целевой → рабочий порог = контрольный + `Надбавка` (5 %); иначе рабочий порог = целевая |
| 13 | R14.1 | …и чтобы порог пересчитывался, пока вытяжка работает | событие любого из двух датчиков влажности переоценивает выключение и скорость |
| 14 | R16 | Как пользователь, я вешаю автоматику на «рубильник» | `Разрешение автоматики` + `Инвертировать` — поведение как в эталоне (§7) |
| 15 | R17 | Как пользователь, я управляю вытяжкой руками, и сценарий это учитывает | три слота `Ручной вход`: выключатель, кнопка, импульсы, контакт — семантика §7 |
| 16 | R17.1 | …и могу запретить и ручные входы, пока автоматика запрещена | `Также не реагировать на ручные входы` (§7) |
| 17 | G01 | Как владелец ванной, я хочу, чтобы вытяжка включалась от пара, даже если датчик присутствия меня не увидел | `Включать по влажности без присутствия` (по умолчанию ОТКЛ): влажность ≥ рабочий порог + `Запас` → включение |
| 18 | G02 | Как пользователь, я хочу форсаж, пока сыро, и тишину, когда уже нет | `Форсаж при высокой влажности`: `RotationSpeed` = скорость форсажа, пока влажность высокая, иначе обычная |
| 19 | G03 | Как пользователь, я хочу узнать, что вытяжка отработала предел и не высушила | предел истёк и влажность выше рабочего порога → уведомление в выбранные каналы |
| 20 | G04 | Как пользователь, я не хочу, чтобы реле щёлкало на «зашёл-вышел» | `Минимальное время работы` (0 = выкл): раньше этого срока авто-выключения нет |
| 21 | G05 | Как пользователь, я не хочу «вкл-выкл-вкл» на границе порога | `Пауза перед повторным включением` (0 = выкл): после авто-выключения авто-включения нет |
| 22 | G06 | Как владелец туалета без датчика движения, я использую дверной контакт | `Инвертировать датчик открытия`: «Закрыто» = занято |
| 23 | R26i | Как пользователь, я не хочу, чтобы сухая комната мешала обычной логике | влажность уже ≤ рабочего порога → условие выполнено, работает таймер присутствия |
| 24 | R27i | Как пользователь, я хочу, чтобы вернувшийся человек не обрывал досушку | активность вернулась → таймер выключения сброшен, досушка продолжается, предельный таймер не сбрасывается |
| 25 | R28i | Как пользователь, я хочу предсказуемого поведения при мёртвом датчике влажности | значение не читается → условие по влажности считается выполненным, работает таймер присутствия, запись в отладочный лог |
| 26 | R25i | Как пользователь, я хочу, чтобы перезапуск хаба не оставил вытяжку навсегда включённой | `onStart: true`: подписки восстанавливаются, у включённой вытяжки предельный таймер заводится заново от момента старта |
| 27 | R29i | Как пользователь, я включаю отладку и вижу, почему сценарий так решил | `Режим отладки` — сообщения в лог, как в эталоне |
| 28 | R21i | Как пользователь, я читаю README и понимаю настройку | README со всеми обязательными разделами |
| 29 | R22i | Как автор, я прогоняю тесты сценария | `.tests/config.json`, тесты от этой спецификации, `preset.json` |
| 30 | R23i | Как автор, я публикую сценарий одной командой | `publish.json` + `./publish ExhaustFanAutomation` проходит |

## Решения по реализации

### 1. Привязка и запись состояния

`sourceServices: [HS.Switch, HS.FanBasic, HS.Fan]`, `sourceCharacteristics: [HC.On, HC.Active]`.
Почему именно эти три: пользователь назвал «сервис выключатель, вентилятор»; в Sprut.Hub
«вентилятор» — это два разных типа, `Fan` (требует `Active`) и `FanBasic` (требует `On`).
Прецедент нескольких пар «сервис + характеристика» в одном `info` — `CodeLock`.
`Outlet` намеренно **не** включён: в брифе розетки нет, а эталон тоже ограничен двумя типами.

Абстракция над этим — ровно одна пара функций: `readFanOn(service)` и `writeFanOn(service, on)`.
Они выбирают `HC.On` или `HC.Active` по тому, какая характеристика есть у сервиса; `Active`
читается как `=== 1`, пишется как `1 / 0`. Значение из `trigger` нормализуется
`normalizeOnValue(value)` → `value === true || value === 1`.

### 2. Активность датчиков

Три слота, типы `MotionSensor` (`MotionDetected === true`), `OccupancySensor`
(`OccupancyDetected === 1`), `ContactSensor` (`ContactSensorState === 1` — «Открыто»).
Опция `Инвертировать датчик открытия` меняет условие контакта на `=== 0` (G06).
Почему один флаг на все слоты, а не на каждый: слотов три, комбинаций восемь,
а реальный случай один — «дверь закрыта = занято».

`computeOccupancyActive(options)` → `true`, если активен хотя бы один из выбранных датчиков.
Если ни один слот не заполнен — всегда `false` (сценарий тогда живёт на влажности и ручных входах).

### 3. Накопление присутствия (R03/R04, «с допуском»)

Три переменные: `presenceSinceAt` (когда началась текущая сессия присутствия),
`onDelayTimerId` (таймер включения), `presenceResetTimerId` (таймер сброса сессии).

- Активность появилась → если `presenceResetTimerId` есть, снять его; если `presenceSinceAt`
  пуст — записать сейчас и завести `onDelayTimerId` на `Задержка включения`.
- Активность пропала (все датчики неактивны) → завести `presenceResetTimerId`
  на `Задержка выключения`.
- `presenceResetTimerId` сработал → `presenceSinceAt = undefined`, снять `onDelayTimerId`.
- `onDelayTimerId` сработал → попытка авто-включения (§5).
- `Задержка включения` = 0 → включение при первой же активности, без накопления.

Почему так, а не «суммарно за окно»: при `Задержка выключения` = 300 с окно допуска и так
получается широким, а объяснить правило в README можно одной строкой.

### 4. Влажность

`readHumidity(uuid)` → число или `undefined` (сервис не выбран, не найден, значение не число).

**Как читается требование про контрольный датчик.** Бриф: «Если влажность на нём равна или
выше указанной целевой влажности, то включаем вытяжку до уровня контрольный +5 %».
Прочтение: контрольный датчик стоит в сухой комнате и задаёт достижимый минимум, поэтому
он меняет **порог выключения**, а не добавляет повода для включения. Буквальное прочтение
(«контрольный ≥ целевой → включить вытяжку») означало бы, что в сырую погоду вытяжка
работает всегда, независимо от того, что происходит в санузле, — это противоречит остальной
задаче. Повод для включения по влажности есть ровно один и он отдельный — G01 (§5).

Рабочий порог (R14):

```
function computeEffectiveTarget(options) {
    const target = options.targetHumidity;
    const ref = readHumidity(options.referenceHumiditySensor);
    if (ref === undefined || ref < target) return target;
    return Math.min(100, ref + options.referenceDelta);
}
```

**Когда пересчитывается (R14.1).** Нигде не кэшируется: функция вызывается заново при каждой
проверке. Проверка запускается по событию `CurrentRelativeHumidity` **любого** из двух
датчиков — комнатного и контрольного, — а также при включении, при срабатывании таймера
выключения и при срабатывании предельного таймера. Событие контрольного датчика делает
то же, что событие комнатного: переоценивает выключение (§8) и скорость (§6).

`isHumiditySatisfied()` → `true`, если датчик влажности не выбран **или** значение не читается
(R28i) **или** значение ≤ рабочего порога. Почему «не читается = выполнено»: иначе мёртвый
датчик держал бы вытяжку до предельного таймера каждый раз.

`isHumidityHigh()` → `true`, если значение комнатного датчика ≥ рабочий порог +
`Запас над целевой` (по умолчанию 10 %). Одно понятие обслуживает и включение по влажности
(G01), и форсаж (G02) — вместо двух почти одинаковых опций.

### 5. Включение

Авто-включение выполняется, если **все** условия истинны:

1. автоматика разрешена (§7);
2. вытяжка выключена;
3. не идёт пауза после авто-выключения (G05, `cooldownUntil`) — по её истечении проверка
   запускается сама, см. §8;
4. не стоит блокировка `Не включать после ручного выключения` (§7);
5. не стоит блокировка после предельного таймера (§8);
6. есть повод — сработал таймер накопления присутствия (§3) **или** `isHumidityHigh()` при
   включённой опции `Включать по влажности без присутствия` (G01);
7. не действует запрет режима «Не включать при присутствии» (§11) — при активном присутствии
   он отменяет и повод по присутствию, и повод по влажности.

При включении: `writeFanOn(true)`, `runStartedAt = now`, завести предельный таймер, применить
скорость (§6), запомнить `lastSensorAutoOnAt` для антидребезга ручных входов. Если в момент
включения активности датчиков нет (включение по влажности или ручное) — таймер выключения
заводится сразу, как в эталоне.

### 6. Скорость (G02)

Если `Форсаж при высокой влажности` включён и у сервиса есть `RotationSpeed` — записывать
`Скорость форсажа` при `isHumidityHigh()` и `Обычная скорость` иначе; пересчёт при каждом
событии любого датчика влажности и при включении. Если характеристики нет — опция не
действует, одна строка в отладочный лог. Скорость никогда не пишется, когда вытяжка выключена.

### 7. Разрешение автоматики и ручные входы (R16/R17)

Семантика эталона, изложенная здесь полностью — чтобы тесты писались от спецификации,
а не от чужого исходника.

**Разрешение автоматики.** `gateAutoSwitch` — выключатель (`HS.Switch`/`HC.On`).
`isAutomationAllowed()` = поле пустое → `true`; иначе значение выключателя, а при
`gateAutoSwitchInvert` — его отрицание. Пока автоматика запрещена: авто-включение не
выполняется; уже работающая вытяжка **не** гасится; таймеры продолжают работать.
Переход выключателя в «разрешено» при активных датчиках — повод попробовать авто-включение.

**Ручные входы.** Три слота, тип определяется по типу выбранного сервиса:

| Тип сервиса | Характеристика | Поведение |
|---|---|---|
| `Switch` | `On` | Вытяжка повторяет состояние. Пока хотя бы один такой выключатель в `On` — авто-выключение, предельный таймер и минимальное время не действуют. Перевод в `Off` → выключение сразу, если ни один другой ручной выключатель не в `On` |
| `ContactSensor` | `ContactSensorState` | Кнопка: только `1` («Открытие») переключает вытяжку, `0` игнорируется |
| `StatelessProgrammableSwitch` | `ProgrammableSwitchEvent` | Только `0` («Одиночное нажатие») переключает вытяжку |
| `C_PulseMeter` | `C_PulseCount` | Любое значение `> 0` переключает вытяжку |

**`Также не реагировать на ручные входы`** (`gateBlocksManualInputs`). Действует только при
выбранном `gateAutoSwitch`. Пока автоматика запрещена, события всех трёх слотов игнорируются
полностью — вытяжка по ним не включается и не выключается. Прямое изменение самого
привязанного сервиса (приложение, сцена, физическая кнопка на вытяжке) продолжает работать.
События, пришедшие во время запрета, задним числом не применяются.

**`Не отключать автоматически после ручного включения`** (`noAutoOffWhenManualOn`).
Если включено, то при включении вытяжки кнопкой, импульсом, контактом или извне (приложение,
сцена, голос) поднимается флаг `manualHold`: обычный таймер выключения не гасит вытяжку,
остаётся только предельный таймер (§8). Флаг снимается при любом выключении вытяжки.
Ручной вход типа `Switch` `manualHold` не поднимает — у него свой механизм удержания.

**`Не включать автоматически, если выключили вручную`** (`noAutoOnAfterManualOff`).
Если включено и вытяжку выключили вручную или извне, пока ещё есть повод работать, ставится
блокировка авто-включения. Снимается в тот момент, когда вытяжка погасла бы сама: все датчики
неактивны, прошла `Задержка выключения` **и** `isHumiditySatisfied()`. Возврат активности до
истечения таймаута сбрасывает отсчёт. Блокировка также снимается при любом включении вытяжки.
К ручному входу типа `Switch` не относится.

**Антидребезг** (`ignoreManualWithin5sAfterSensorOn`, по умолчанию ВКЛ). В течение 5 секунд
после авто-включения события кнопки, импульса и контакта игнорируются полностью. Ручной
выключатель продолжает работать.

### 8. Выключение

Авто-выключение выполняется, когда **все** условия истинны:
все датчики неактивны дольше `Задержка выключения` · `isHumiditySatisfied()` · прошло
`Минимальное время работы` (G04) · ни один ручной выключатель не в `On` · не поднят `manualHold`.

**Предельный таймер** (R11, R24i). Отсчёт от `runStartedAt`. Срабатывание выключает вытяжку,
**не** проверяя влажность, присутствие и минимальное время. Единственное исключение —
ручной выключатель в `On` (§7). Предел действует на любое включение: по присутствию, по
влажности и ручное, — это ответ пользователя «один общий лимит»; в брифе предел был привязан
только к досушке. `Максимальное время непрерывной работы` = 0 отключает предел.

**Блокировка после предела** (R11.1). Сразу после выключения по пределу авто-включение
запрещено, пока повод не исчезнет естественным путём: нет активности датчиков дольше
`Задержка выключения` **и** `isHumiditySatisfied()`. Без этого вытяжка, выключенная пределом
при живом присутствии, включилась бы обратно на следующем же событии датчика, и предел
не значил бы ничего. Ручное включение блокировку снимает.

**Уведомление** (G03). При срабатывании предельного таймера, если `Уведомлять…` включено,
датчик влажности выбран и его значение выше рабочего порога — вызвать `log.message(...)`.

Доставка — **только `log.message`**, по прямому решению пользователя: «только log.message надо
оставить». Прежний вариант через `Notify` с каналами, клиентами и тихим режимом снят вместе
с опциями `notifyChannels`, `notifyClients`, `notifySilent`; ветка оформления под Telegram
снята следом — распознавать канал больше не по чему, поэтому сообщение одно.

`log.message(format, ...args)` объявлен в `ScenarioTemplate/spruthub.js`, `interface Log`
(`message` / `info` / `warn` / `error`). В репозитории его пока не использует ни один сценарий;
симулятор пишет его отдельным уровнем `message`, поэтому факт отправки проверяется тестом
через `logs.byLevel("message")`.

Сообщение несёт те же факты, что и раньше: влажность, рабочий порог (`computeEffectiveTarget`,
а не значение опции `Целевая влажность` — при контрольном датчике они расходятся), сколько
вытяжка отработала, устройство с идентификатором и комнату.

**Пауза** (G05). После авто-выключения (по таймеру, не по пределу) запускается
`cooldownMinutes`, в течение которых авто-включение не выполняется. Ручное включение работает.
**По истечении паузы сценарий сам проверяет, есть ли повод работать** (присутствие набрано
или высокая влажность при `humidityStartsFan`), и включает вытяжку, не дожидаясь нового
события датчика. Без этого при непрерывном присутствии вытяжка оставалась бы выключенной
до следующего фронта — уточнено после слепой приёмки, таск 04. Блокировки после предельного
таймера это не касается: там возврат не нужен, пока повод не исчез сам.

### 9. Подписки и перезапуск (R25i)

`Hub.subscribeWithCondition` на типы `MotionSensor`, `OccupancySensor`, `ContactSensor`,
`HumiditySensor`, `Switch`, `StatelessProgrammableSwitch`, `C_PulseMeter` и характеристики
`MotionDetected`, `OccupancyDetected`, `ContactSensorState`, `CurrentRelativeHumidity`, `On`,
`ProgrammableSwitchEvent`, `C_PulseCount`. Событие учитывается, только если UUID сервиса
совпадает с одним из выбранных в опциях; совпадения по типу недостаточно.

**Защита от «зависших» подписок** — механизм эталона. При пересохранении сценария хаб
исполняет скрипт заново со свежим `variables`, но колбэк прежней подписки остаётся жив со
старым `variables`. Поэтому при создании подписки счётчик `global["EFA_subGen_" + uuid]`
инкрементируется, а колбэк запоминает своё «поколение»; колбэк, чьё поколение меньше
текущего, немедленно возвращается. Если `global` недоступен — защита просто отключается.

`onStart: true`. При старте: подписка создаётся заново; если вытяжка включена —
`runStartedAt = now` и предельный таймер заводится заново (реальное время включения хабу
неизвестно), заводится таймер выключения, если активности нет; если выключена и активность
есть — начинается накопление присутствия.

### 10. Опции (порядок в UI)

| Группа | Опция | Тип | По умолчанию |
|---|---|---|---|
| ОПИСАНИЕ | `desc` | status | — |
| ДАТЧИКИ АКТИВНОСТИ | `motion1…3` | list | «» |
| | `contactInverted` | Boolean | `false` |
| ВЛАЖНОСТЬ | `humiditySensor` | list | «» |
| | `targetHumidity` | Integer 0…100 | `60` |
| | `referenceHumiditySensor` | list | «» |
| | `referenceDelta` | Integer 0…50 | `5` |
| | `humidityHighDelta` | Integer 0…50 | `10` |
| | `humidityStartsFan` | Boolean | `false` |
| СКОРОСТЬ | `boostEnabled` | Boolean | `false` |
| | `normalSpeed` | Integer 1…100 | `50` |
| | `boostSpeed` | Integer 1…100 | `100` |
| РУЧНЫЕ ВХОДЫ | `manualControl1…3` | list | «» |
| РАЗРЕШЕНИЕ АВТОМАТИКИ | `gateAutoSwitch` | list | «» |
| | `gateAutoSwitchInvert` | Boolean | `false` |
| | `gateBlocksManualInputs` | Boolean | `false` |
| РУЧНОЕ УДЕРЖАНИЕ | `noAutoOffWhenManualOn` | Boolean | `false` |
| | `noAutoOnAfterManualOff` | Boolean | `false` |
| | `ignoreManualWithin5sAfterSensorOn` | Boolean | `true` |
| ТАЙМЕРЫ | `onDelaySeconds` | Integer 0…86400 | `120` |
| | `offDelaySeconds` | Integer 0…86400 | `300` |
| | `minRunMinutes` | Integer 0…1440 | `0` |
| | `cooldownMinutes` | Integer 0…1440 | `0` |
| | `maxRunMinutes` | Integer 0…10080 | `180` |
| РЕЖИМ ПРИСУТСТВИЯ | `noRunWhilePresent` | Boolean | `false` |
| | `airingMinutes` | Integer 0…1440 | `5` |
| ПЕРИОДИЧЕСКАЯ ВЕНТИЛЯЦИЯ | `airingIntervalHours` | Integer 0…168 | `0` |
| УВЕДОМЛЕНИЯ | `notifyOnDryTimeout` | Boolean | `false` |
| ПРОЧЕЕ | `debug` | Boolean | `false` |

`0` в `minRunMinutes`, `cooldownMinutes`, `maxRunMinutes` = функция выключена.
Каждая опция получает двуязычные `name`/`desc`.

**Что из эталона НЕ переносится и почему:**

| Из эталона | Почему нет |
|---|---|
| `luxSensor`, `maxAmbientLux` (освещённость) | вытяжке безразлична освещённость |
| `manualHoldSafetyOffDelayMinutes` | заменён общим `maxRunMinutes` — ответ пользователя «один общий лимит» |
| привязка к `Lightbulb` | вытяжка не лампа; вместо неё `Fan` и `FanBasic` |

### 11. Режим «Не включать при присутствии» (G07…G11)

Инверсия логики присутствия, по брифу 2026-09-23. По умолчанию выключен — базовое
поведение §3–§8 не меняется ни на шаг.

Две опции: `noRunWhilePresent` (Boolean, `false`) и `airingMinutes` (Integer 0…1440, `5`; она же обслуживает §12).

Когда `noRunWhilePresent` включён:

1. **Пока присутствие активно — вытяжка выключена.** Авто-включение не выполняется; если
   вытяжка работает (в том числе проветривает), она выключается сразу.
   **Исключение, названное явно:** это правило исполняется по событиям датчиков и на старте хаба.
   Если человек уже внутри и сам включает вытяжку — кнопкой, из приложения, сценой, — она
   остаётся работать: он нажал при себе, значит хотел. Асимметрия с пунктом 9 намеренная:
   там присутствие *приходит* к работающей вытяжке, здесь работа *приходит* к присутствию.
   Первое — в брифе («если опять зашёл и работала - выключаем»), второе в брифе не названо,
   и §8 «запрет — на автоматику, а не на человека» решает его в пользу человека. Это сильнее влажности:
   «запрет сильнее» — ответ пользователя. `humidityStartsFan` при активном присутствии
   не срабатывает.
2. **Присутствие пропало и подтверждено** `offDelaySeconds` → включение на `airingMinutes`,
   затем выключение. `Задержка выключения` в этом режиме продолжает значить ровно то,
   что и раньше: подтвердить, что человек действительно ушёл, а не что моргнул PIR.
3. **Визит засчитывается, только если присутствие держалось ≥ `onDelaySeconds`.** Заглянул
   на десять секунд — продувки нет. Опция сохраняет прежний смысл, меняется только то,
   что она открывает: не включение при человеке, а право на продувку после него.
4. **Присутствие вернулось во время продувки → выключение сразу.** Дословно: «если опять
   зашёл и работала - выключаем». Это сильнее `minRunMinutes`.
5. **Влажность продувку не заканчивает** — ответ пользователя «только время продувки».
   `isHumiditySatisfied()` в этом режиме на выключение не влияет.
6. `maxRunMinutes` остаётся верхним пределом и здесь.
7. `airingMinutes` = 0 → продувки нет: режим сводится к «никогда не включаться по присутствию».
8. **Ручные входы работают как обычно.** Запрет — на автоматику, а не на человека.
9. **Присутствие гасит вытяжку независимо от того, что её включило** — дословно по брифу:
   «если опять зашёл и работала - выключаем», без оговорок про источник. Исключение одно —
   действующее ручное удержание: ручной вход типа `Выключатель` в `On` (вытяжка повторяет его
   положение, §7) или поднятый `manualHold` при включённой опции `Не отключать автоматически
   после ручного включения`. Оба — явные механизмы «держать включённой», настроенные
   пользователем, и режим их не отменяет.
   *Ревизия: здесь сначала было записано обратное — «гасим только то, что запустила
   автоматика». Это была отсебятина поверх слов пользователя, код оказался прав.*
10. **Окончание продувки по времени — это авто-выключение**, поэтому запускает `cooldownMinutes`
    как любое другое. Дёрганье на границе гасится им же.
11. **Пока `Разрешение автоматики` запрещает автоматику, режим не делает ничего**: ни продувки,
    ни принудительного выключения при присутствии. Это согласуется с §7, где запрет не гасит
    уже работающую вытяжку, а действует только на новые попытки авто-включения. Иначе вышла бы
    несимметричность: сценарий не может включить вытяжку, но может её выключить.
12. **`Минимальное время работы` больше `Время продувки` продлевает продувку** — минимальное
    время это нижняя граница любого сеанса, и продувка не исключение. Механизм: выключение
    сдвигается общим путём авто-выключения до конца минимального времени, таймер продувки
    заново не заводится.
13. **Продувка сильнее повода по влажности в момент включения.** Если есть и право на продувку,
    и высокая влажность при `humidityStartsFan`, сеанс ведётся как продувка: фиксированные
    `airingMinutes`, влажность её не продлевает (пункт 5). Работа по влажности может начаться
    заново следующим событием датчика или по истечении паузы.
14. **При старте хаба с включённой вытяжкой и активным присутствием вытяжка гасится**, и
    начинается сессия присутствия — если она ещё не начата. Источник включения после перезапуска
    неизвестен, и смысл режима перевешивает: человек внутри — вытяжка молчит. При запрещённой
    автоматике этот путь тоже молчит (пункт 11): вытяжка остаётся включённой, как в §7.

**Следствие, которое надо назвать в README:** если вместе с этим режимом включён
`humidityStartsFan`, то после ухода человека вытяжка может продолжить или снова начать
работу по влажности — это и есть смысл той опции, и её отсчёт независим от продувки.
Дёрганье на границе гасится `cooldownMinutes`.

### 12. Периодическая вентиляция (G12…G16)

По брифу 2026-09-23 (второе дополнение). По умолчанию выключена.

Одна новая опция `airingIntervalHours` (Integer 0…168, `0`). `0` — не включать автоматически;
рекомендованное значение названо пользователем — 6, и оно идёт в `desc` как рекомендация,
а не как дефолт.

**Длительность берётся из общей опции `airingMinutes`** (Integer 0…1440, `5`) — это переименованный
`airingMinutes` из §11. Одно понятие «сколько проветривать» с двумя поводами: продувка после ухода
(§11) и периодическая вентиляция. Пользователь просил «по дефолту через 5 минут выключается» —
ровно текущий дефолт, поэтому вторая опция на то же самое не заводится.

Правила:

1. **Отсчёт идёт от момента, когда вытяжка последний раз выключилась.** Дословно по брифу:
   «только если до этого она не включалась по присутствию или вручную». То есть любая работа —
   по присутствию, по влажности, ручная, продувка — обнуляет отсчёт, и следующая периодическая
   вентиляция будет через `airingIntervalHours` после её окончания.
2. **Срабатывает только при отсутствии присутствия.** Активные датчики — повода нет.
3. Включение на `airingMinutes`, затем выключение — как продувка в §11.
4. **Это автоматика**, поэтому подчиняется `Разрешение автоматики` (§7), паузе `cooldownMinutes`,
   пределу `maxRunMinutes` и нижней границе `minRunMinutes` — как любое авто-включение.
5. **С включённым режимом §11**: если присутствие появилось во время периодической вентиляции,
   вытяжка выключается по §11.1 — отдельного правила не нужно.
6. **Перезапуск хаба обнуляет отсчёт**: `variables` не переживают перезапуск, поэтому первая
   периодическая вентиляция случится через `airingIntervalHours` после старта. Это назвать
   в README, чтобы не выглядело пропажей.
7. `airingIntervalHours` = 0 → механизм выключен целиком, таймер не заводится.
8. Влажность периодическую вентиляцию не продлевает и не обрывает — как в §11.5.
9. **Активность в момент истечения интервала — случай пропускается**, отсчёт идёт заново на
   полный интервал. Вентиляция не «ждёт в очереди» спада активности: комната и так проветривается
   работой по присутствию, а очередь означала бы включение сразу после ухода человека.
10. **Если по окончании проветривания выключить не дали, потому что вернулась активность** —
    сеанс перестаёт считаться проветриванием и уходит под обычный таймер выключения. Иначе его
    нечем было бы закончить, кроме предельного таймера.
    *Ревизия: сначала здесь было названо и ручное удержание. Это ошибка — под удержанием
    обычный таймер тоже бессилен (§7), поэтому сеанс законно заканчивается предельным таймером,
    и два поведения через шов неразличимы. Удержание, к тому же, поднимается только включением
    вытяжки, а во время проветривания она уже включена, так что внутри сеанса поднять его нечем.*
10a. **Продувка одна на визит и сама себя не перезапускает.** Право на неё даёт один
    подтверждённый уход после засчитанного визита; следующая продувка будет за следующий визит.
10a2. **Принудительное выключение при присутствии паузу не заводит.** `cooldownMinutes`
    существует против дёрганья на границе порога, а здесь цикл ровно один: человек вошёл —
    выключили, вышел — проветрили. Если бы пауза заводилась, она заблокировала бы проветривание
    за тот самый визит, ради которого режим и нужен. При дефолте `0` разницы нет, при
    ненулевом — была бы.
10b. **Две просьбы одновременно — остаётся последняя.** Если визит закончился в режиме §11
    и тут же истёк интервал, просьба о периодическом проветривании затирает просьбу за визит.
    Наблюдаемой разницы нет: сеанс один и той же длительности `airingMinutes`, меняется только
    текст в отладочном логе. Правило названо, чтобы затирание не приняли за дефект.
11. **Просьба о вентиляции живёт до ближайшей попытки авто-включения.** Если в момент истечения
    интервала автоматика запрещена «рубильником», вентиляция случится, когда её разрешат.
    Так же ведёт себя отложенная продувка §11.

## Границы и швы

Сценарий — один файл: движок Nashorn не знает модулей, а эталон целиком лежит в одном
`source/*.js`. «Модули» ниже — группы функций внутри файла; границы между ними нужны,
чтобы тесты и ревью говорили об одном и том же.

| Модуль | Владеет | Выставляет | Прячет |
|---|---|---|---|
| `info` + `createOptions()` | метаданными и списком опций | `info`, `createOptions()` | сборку списков устройств `collectServicesByTypes` |
| `output` | записью в привязанный сервис | `readFanOn(svc)`, `writeFanOn(svc, on, options, src)`, `applyFanSpeed(svc, options, src)` | выбор `On`/`Active`, наличие `RotationSpeed` |
| `presence` | накоплением присутствия | `computeOccupancyActive(options)`, `onOccupancyChanged(variables, options, src)` | три таймера и семантику «с допуском» |
| `humidity` | влажностью и порогами | `readHumidity(uuid)`, `computeEffectiveTarget(options)`, `isHumiditySatisfied(options)`, `isHumidityHigh(options)` | контрольный датчик, клампинг, деградацию при мёртвом датчике |
| `decide` | решением вкл/выкл | `tryAutoTurnOn(...)`, `tryAutoTurnOff(...)` | порядок проверок, паузу, минимальное время, блокировки |
| `timers` | таймерами | `armOffTimer`, `clearOffTimer`, `armMaxRunTimer`, `clearMaxRunTimer`, `armCooldown` | идентификаторы в `variables` |
| `manual` | ручными входами и «рубильником» | `isAutomationAllowed(options)`, `handleManualControlEvent(...)` | типы входов и антидребезг |
| `notify` | уведомлением по таймауту | `sendDryTimeoutNotification(options, src)` | разбор каналов/клиентов |
| `log` | отладкой | `logInfo`, `logError` | префикс и ленивые сообщения |

**Шов для тестов ровно один** — DSL ScenarioSimulator: `scenario.info()` и
`scenario.run({source, value, variables, options, context})` плюс виртуальные устройства пресета.
Внутренние функции не тестируются напрямую: они не выставлены наружу файла и
меняются вместе с реализацией. Исключение — `collectServicesByTypes`, которая в эталоне уже
покрыта отдельным файлом тестов; здесь она такая же и покрывается так же.

## Вне рамок

| Требование | Почему не сейчас |
|---|---|
| R07 — «например сценарий в туалете» | это пример применения, а не требование; попадает в README как пример настройки |
| R18 — рассмотрено и отклонено: ограничение по уличной температуре/влажности | роль «внешнего опорного значения» уже играет контрольный датчик |
| R18 — рассмотрено и отклонено: расписание «тихих часов» | решается существующим `Разрешение автоматики` + виртуальный выключатель |
| R18 — рассмотрено и отклонено: несколько вытяжек из одного экземпляра | один экземпляр — один привязанный сервис, как в эталоне |

## Открытые места

Нет. Все `placeholder`-кандидаты закрыты в брифинге; фактов о пользователе
(тексты, адреса, счета) сценарий не требует.

## Покрытие манифеста

| Требование | Раздел спецификации |
|---|---|
| R01 | История 1, Решения §1–§10 |
| R02 | История 2, Решения §1 |
| R03 | Истории 4–6, Решения §3 |
| R04 | История 4, Решения §3, §10 |
| R05 | История 2, Решения §1 |
| R06 | История 7, Решения §3, §8 |
| R07 | Вне рамок (пример в README) |
| R08 | История 8, Решения §4 |
| R09 | История 9, Решения §4, §8 |
| R10 | История 8, Решения §4, §10 |
| R11 | Истории 10–11, Решения §8 |
| R12 | История 10, Решения §10 |
| R13 | Истории 12–13, Решения §4 |
| R14 | Истории 12–13, Решения §4 |
| R15 | История 12, Решения §10 |
| R16 | История 14, Решения §7 |
| R17 | Истории 15–16, Решения §7 |
| R18 | Истории 17–22 (G01…G06) + Вне рамок |
| R19 | История 1, Решения §7, §9, §10 |
| R20i | История 3, Решения §2 |
| R21i | История 28 |
| R22i | История 29 |
| R23i | История 30 |
| R24i | История 10, Решения §7, §8 |
| R25i | История 26, Решения §9 |
| R26i | История 23, Решения §4 |
| R27i | История 24, Решения §3, §8 |
| R28i | История 25, Решения §4 |
| R29i | История 27, Решения §10 |
| G01 | История 17, Решения §4, §5 |
| G02 | История 18, Решения §6 |
| G03 | История 19, Решения §8 |
| G04 | История 20, Решения §8 |
| G05 | История 21, Решения §5, §8 |
| G06 | История 22, Решения §2 |
| G07 | Решения §11 (все пункты), §10 |
| G08 | Решения §11.1 |
| G09 | Решения §11.2, §11.3 |
| G10 | Решения §11.4, §11.9 |
| G11 | Решения §11 — пример «туалет» в README |
| G12 | Решения §12, §10 |
| G13 | Решения §12.1, §12.2 |
| G14 | Решения §12 (дефолт 0, рекомендация 6), §10 |
| G15 | Решения §12.3, общая опция `airingMinutes` §10 |
| G16 | Решения §12.1 — отсчёт от последнего выключения |
