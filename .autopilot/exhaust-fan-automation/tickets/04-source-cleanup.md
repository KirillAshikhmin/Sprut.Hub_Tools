# 04 — Чистка исходника по отложенным находкам ревью

**Требования:** R19, R25i
**Blocked by:** —
**Зона:** `ExhaustFanAutomation/source/`
**Волна:** 3
**Status:** ready

## Что должно заработать

Поведение сценария не меняется ни в одну сторону — меняется то, на чём оно держится.
Сейчас состояние сеанса сбрасывается в четырёх местах мимо `endRun`, а колбэк таймера
устаревшего поколения живёт до своего срока (у предельного таймера это до 7 суток)
и просто ничего не делает, вместо того чтобы быть снятым.

## Из брифа, дословно

> «Напиши новый сценарий аналогичный Свету по датчику движения»
> «Эталон есть.»

## Разделы спецификации

`spec.md` §3, §5, §8, §9 и «Границы и швы». Поведение, описанное там, должно остаться
ровно таким же — это рефакторинг, а не смена контракта.

## Критерии приёмки

- [ ] `beginRun` / `endRun` — единственные места, где поля сеанса выставляются и гасятся; `manualHold` входит в `endRun`, вызывающие его сами не трогают
- [ ] Таймер устаревшего поколения снимается, а не молча ничего не делает: висящих колбэков после пересохранения сценария не остаётся
- [ ] Все 138 тестов сценария и весь репозиторий (3814) остаются зелёными — поведение не изменилось
- [ ] `jjs -scripting --language=es6 -co` чисто
