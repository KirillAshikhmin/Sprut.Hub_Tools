# 07 — Сжатие: убрать дублирование и лишние комментарии

**Требования:** R19
**Blocked by:** —
**Зона:** `ExhaustFanAutomation/source/`
**Волна:** 5
**Status:** ready

## Что должно заработать

Ничего нового. Файл 1964 строки против 1089 у эталона; часть прироста заслуженная
(семь механизмов, которых у света нет), часть — дублирование и комментарии.
Поведение обязано остаться побайтно тем же на всех 135 тестах сценария.

## Из брифа, дословно

> «Напиши новый сценарий аналогичный Свету по датчику движения»
> «Эталон есть.»

## Разделы спецификации

`spec.md` §2, §3, §5, §8, §9 и «Границы и швы». Ни одно решение оттуда не меняется.

## Критерии приёмки

- [ ] Решение «нужен ли таймер выключения» принимается в одной функции; `syncOffTimerWhenFanOnWithoutOccupancy`, хвост `manualTurnOn`, хвост `tryAutoTurnOn` и проверка внутри `armOffTimer` её зовут, а не повторяют
- [ ] Активные значения датчиков заданы одной картой «тип сервиса → характеристика + активное значение»; `computeOccupancyActive` и подписка читают её, а не кодируют правило каждая по-своему
- [ ] `PRESENCE_READY_TOLERANCE_MS` удалена: факт «присутствие набрано» имеет один источник истины, допуск не нужен
- [ ] `clearAutoOnLockTimer` либо называет оба своих эффекта в имени, либо перестаёт трогать `autoOnLockSilent` — флаг меняется только в явных переходах блокировки
- [ ] Признак первого вызова живёт в собственном поле `variables`, выставляемом отдельно от подписки: порядок строк перестаёт быть условием корректности, а событие, пришедшее раньше стартового вызова, за запуск не принимается
- [ ] Комментарии проредены: остаются те, что объясняют «почему», уходят те, что пересказывают соседнюю строку
- [ ] `bun run cli run --root ..` → 3820 passed, 0 failed; `bun run cli run ExhaustFanAutomation --root ..` → 135 passed
- [ ] `jjs -scripting --language=es6 -co` чисто
- [ ] Файл стал короче минимум на 150 строк
