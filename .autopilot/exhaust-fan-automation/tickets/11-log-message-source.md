# 11 — Уведомление через log.message, Notify снять

**Требования:** G03, R19
**Blocked by:** —
**Зона:** `ExhaustFanAutomation/source/`, `ExhaustFanAutomation/README.md`
**Волна:** 8
**Status:** ready

## Что должно заработать

Уведомление «комната не высохла» уходит через `log.message`. `Notify` не используется
вовсе; три опции доставки и ветка оформления под Telegram снимаются.

## Из брифа, дословно

> «Уведомление, если не высохло» (выбрано в брифинге)

Решение пользователя по доставке, дословно: «только log.message надо оставить».

## Разделы спецификации

`spec.md` §8, абзац «Уведомление» — переписан под это решение. Таблица §10, блок `УВЕДОМЛЕНИЯ`
теперь содержит одну опцию.

## Критерии приёмки

- [ ] Отправка идёт через `log.message(...)`; обращений к `Notify` в файле не осталось
- [ ] Опции `notifyChannels`, `notifyClients`, `notifySilent` удалены; `notifyOnDryTimeout` остаётся
- [ ] Ветка оформления под Telegram, распознавание канала и константа префикса удалены — сообщение одно
- [ ] Состав фактов прежний: влажность, рабочий порог, сколько отработала, устройство с идентификатором, комната. Ярлык порога по-прежнему называет **рабочий** порог
- [ ] Условие отправки прежнее: предел сработал, датчик влажности выбран, значение выше рабочего порога, опция включена
- [ ] Осиротевшие после удаления хелперы сняты, а не оставлены мёртвыми
- [ ] README: три опции убраны из «Параметров», раздел про уведомления переписан — ни слова про каналы, клиентов, тихий режим и Telegram
- [ ] `jjs -co` чисто; прогон без новых падений (тесты чинит параллельный таск)
