# 12 — Тесты и пресет под log.message

**Требования:** R22i, G03
**Blocked by:** —
**Зона:** `ExhaustFanAutomation/.tests/`
**Волна:** 8
**Status:** ready

## Что должно заработать

Тесты проверяют доставку через `log.message` вместо `Notify`; тесты на тихий режим
и на ветвление формата уходят вместе с механизмами, которых больше нет.

## Из брифа, дословно

> «Уведомление, если не высохло» (выбрано в брифинге)

Решение пользователя, дословно: «только log.message надо оставить».

## Разделы спецификации

`spec.md` §8 (переписан) и §10. `ScenarioSimulator` пишет `log.message` уровнем `message`,
достаётся через `logs.byLevel("message")`.

## Критерии приёмки

- [ ] Тест на факт отправки: повод есть → ровно одна запись уровня `message` с фактами (влажность, рабочий порог, минуты, устройство, комната)
- [ ] Тест на отрицание: `notifyOnDryTimeout` выключено → записи уровня `message` про недосушку нет
- [ ] Тесты на `notifySilent` и на ветвление формата удалены вместе с опциями, а не оставлены падающими
- [ ] Контрактный тест опций §10 снова полон: трёх удалённых опций в нём нет, `notifyOnDryTimeout` есть
- [ ] `preset.json`: три опции убраны
- [ ] `.tests/SPEC.md` приведена в соответствие: доставка через `log.message`, ни слова про каналы и Telegram
