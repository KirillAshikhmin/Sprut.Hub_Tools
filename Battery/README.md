# Отслеживание батарей умных устройств и сводка по ним
# Логический и глобальный сценарии

🔋 Универсальная система мониторинга батарей для всех умных устройств в Sprut.Hub

## 📊 Что делает:
- Автоматически собирает данные о заряде всех устройств с батареями (датчики, гаджеты и т.д.)
- Предупреждает о разряженных батареях с подробной информацией
- Подсчитывает, сколько батареек купить — например: «AAA — 4 шт, CR2032 — 2 шт»
- Фильтрует устройства — можно игнорировать ненужные или проверять только выбранные
- Поддерживает настройку каналов уведомлений и клиентов
- Ведет историю замены/зарядки батарей

## 🔧 Поддерживаемые сервисы:
- **BatteryService** (Сервис батареи)

## ⚠️ ВАЖНО! Порядок настройки
1. **Сначала** импортируйте оба сценария (логический и глобальный)
2. **Настройте уведомления** в коде сценариев
2. **Включите и настройте** логический сценарий для каждого устройства с батареей
3. **Создайте** блочный сценарий для вызова сводки (опционально)

## Установка и настройка

### 1. Импорт сценариев
1. Скачать файлы `BatteryMonitoring.json` и `BatteryMonitoring.Global.json`
2. Импортировать оба сценария (в разделе Сценарии нажать "+" - "импортировать шаблон")
3. Импортировать сначала логический, затем глобальный сценарий

### 2. Предварительная настройка сценариев (ВАЖНО)
#### ВАЖНЫЙ шаг. Если не произвести настройку, тогда уведомления будут приходить в сервис по кмолчанию (Telegram_1 и клиент 1), и после этого придётся менять параметры у каждого устрйоства индивидуально. 
1. Откройте код логического сценария "🔋 Мониторинг батареек" в разделе "Сценарии" - "Логические"
2. Найдите сверху код
```javascript
// Заполните ниже параметры уведомлений по умолчанию, для того, что бы не указывать их в параметрах для каждого устройства
const DEFAULT_NOTIFICATION_CHANNEL = "Telegram_1"; // Канал уведомлений для отправки уведомлений (например, Telegram_1, Web_1), или пустое поле, что бы уведомления отправлялись по всем каналам
const DEFAULT_NOTIFICATION_CLIENTS = "1"; // Клиенты уведомлений которым будут приходить уведомления (например, 1, 3, 4)
const DEFAULT_SILENT_NOTIFICATION = false; // Использовать тихое уведомление. true - тихие, false - со звуком
```
3. В первой строке (DEFAULT_NOTIFICATION_CHANNEL) (после символа = внутри кавычек) укажите нужный вам канал уведомлений (отсюда https://web.spruthub.ru/notifications слева), Например Telegram_1, Web_1. Если оставить поле пустым, то уведомления будут приходить по всем каналам.
4. Во второй строке (DEFAULT_NOTIFICATION_CLIENTS) укажите перечень клиентов (на той же странице справа), которым будет приходить уведомления внутри указанного канала. Например, 1, 3, 4.
5. Откройте код глобального сценария "BatteryMonitoring.Global"
6. Аналогично логического сценария укажите канал и клиентов уведомлений, куда будет приходить сводка в строках, указанных ниже
```javascript
    var defaultNotificationChannel = "Telegram_1";            // Канал уведомлений (например, "Telegram_1", "Web_1") - по умолчанию канал не указан
    var defaultNotificationClients = "1";            // Строка с ID клиентов через запятую (например, "1, 3, 4") - по умолчанию пустая строка
    var defaultSilentNotification = false;          // Отправлять уведомление без звука - по умолчанию уведомления с звуком
```



### 3. Настройка логического сценария
1. Перейти в настройки устройства с батарейкой (шестерёнка справа от названия)
2. Выбрать "Логика" → "Батарейка>" → "🔋 Мониторинг батареек"
3. **АКТИВИРОВАТЬ** переключатель "Активно"
4. **НАСТРОИТЬ ПАРАМЕТРЫ:**
   - **Тип батареи** - выберите из списка или "Другой"
   - **Количество батарей** - сколько батареек в устройстве
   - **Тип зарядки** - если устройство перезаряжается
   - **Расположение** - где находится устройство (опционально)
   - **Комментарий** - дополнительная информация (опционально)
   - **Собственный минимальный уровень заряда** - использовать свой порог вместо стандартного
   - **Минимальный заряд для уведомлений** - при каком уровне отправлять уведомления (если включен собственный порог)
   - **Менять имя сервиса на дату замены батареи** - добавлять дату в название сервиса
   - **Канал уведомлений** - канал для отправки (например, "Telegram_1", "Web_1")
   - **Клиенты уведомлений** - ID клиентов через запятую (например, "1, 3, 4")
   - **Тихое уведомление** - отправлять без звука

### 2. Настройка глобального сценария (опционально)
1. Создать блочный сценарий с планировщиком для вызова сводки (например каждое воскресенье в полдень)
2. Добавить блок "Код" с вызовом функции (одной из):
   ```javascript
   // Полная сводка по всем батареям
   global.getBatteriesInfo(true)
   
   // Или только разряженные батареи
   global.getBatteriesInfo(false)
   
   // С дополнительными параметрами
   global.getBatteriesInfo(
       true,                    // полная информация
       ["13", "44"],           // исключить устройства с ID 13 и 44
       [],                     // белый список (пустой = все устройства)
       "Telegram_1",           // канал уведомлений
       "1, 3, 4",             // клиенты уведомлений
       false                   // не тихое уведомление
   )
   ```

### Функция getBatteriesInfo
```javascript
global.getBatteriesInfo(fullInfo, blackList, whiteList, notificationChannel, notificationClients, silentNotification)
```

**Параметры:**
- `fullInfo` (Boolean) - показывать полную информацию (включая устройства в норме и без логики)
- `blackList` (Array) - список ID устройств для исключения из проверки
- `whiteList` (Array) - список ID устройств для включения в проверку (если пустой, проверяются все)
- `notificationChannel` (String) - канал уведомлений (например, "Telegram_1", "Web_1")
- `notificationClients` (String) - строка с ID клиентов через запятую (например, "1, 3, 4")
- `silentNotification` (Boolean) - отправлять уведомление без звука

## За что отвечают сценарии

### Логический сценарий
- **Автоматически** отслеживает состояние батареи устройства
- **Отправляет уведомления** при разряде батареи
- **Сохраняет данные** о типе батареи, количестве, расположении
- **Ведет историю** замены/зарядки батарей
- **Может работать автономно** без глобального сценария

### Глобальный сценарий
- **Формирует сводку** по всем батареям в системе
- **Поддерживает фильтрацию** устройств (черный/белый список)
- **Настраиваемые каналы** уведомлений
- **Подсчитывает батареи** для замены



### 🎯 Фильтрация устройств
```javascript
// В Global.js можно настроить постоянные списки
const blackListAccessories = ["13", "44"];  // чёрный список (указанные устройства не будут учитываться а сводке)
const whiteListAccessories = ["18", "42"];  // только эти устройства (в сводке будут только эти устройства)
```

### Получение данных о севших батарейках
```javascript
// Получить список батарей для замены
let batteries = GlobalVariables.batteriesForChange
let batKeys = Object.keys(batteries)
if (batKeys.length > 0) {
    batKeys.forEach(function (type) {
        let batteryType = type
        let count = batteries[type]
        console.log("Нужно купить: " + batteryType + " - " + count + " шт.")
    })
}
```

## Переменная GlobalVariables.batteriesForChange
Формируется при вызове глобальной функции сводки. Содержит перечень батарей, которые необходимы для замены и их количество.

**Формат:**
```javascript
{
    "AAA": 2,
    "CR2032": 1
}
```

## Дополнительная информация

### Независимость сценариев
Логический и глобальный могут работать независимо друг от друга, но в паре раскрывается полная функциональность:
- **Логический** отвечает за отправку уведомлений о разряде и сохранение данных
- **Глобальный** формирует сводку о батареях устройств


### Каналы уведомлений
- **Telegram** - форматирование с переносами строк и Markdown разметкой
- **Другие каналы** - обычное текстовое форматирование
- **Клиенты** - можно указать конкретных получателей уведомлений
- **Тихий режим** - уведомления без звука

## История изменений:

### 2.0
- **Полностью переработана система уведомлений** - теперь используется новый API Notify
- **Добавлена поддержка каналов уведомлений** - можно указать конкретный канал (Telegram_1, Web_1 и т.д.)
- **Добавлена поддержка клиентов уведомлений** - можно указать конкретных получателей
- **Добавлен тихий режим** - уведомления без звука
- **Обновлена функция getBatteriesInfo** - добавлены новые параметры для настройки уведомлений
- **Добавлены подробные комментарии** к параметрам функций
- **Улучшена документация** - полное описание установки и использования

### 1.1
- Исправлена отправка уведомлений при разряде батареи (вообще не отправлялись)
- Исправлено формирование отчёта в сводке, теперь форматирование правильное и нет пустых сообщений
- Добавлена опция "Менять имя сервиса", включённая по умолчанию. С ней в имя сервиса Батареи устанавливается дата замены или заряда батареи (при активации сервиса сразу ставится текущая, ибо не знаем когда меняли). Если батарея разряжена, то в сообщении указывается эта дата

### 1.0
- Первая версия

---

# Все сценарии бесплатные и можно свободно использовать как в личных, так и в коммерческих целях.