# 🌅 День-ночь
Логический сценарий

> **Автоматическое включение и отключение выключателя на основе времени восхода и заката солнца**

Логический сценарий для Sprut.Hub, который автоматически включает и отключает выключатель на основе времени восхода и заката солнца по указанным координатам, или по фиксированному времени. Корректно обрабатывает полярный день и полярную ночь.

Не зависит от состояния хаба в момент восхода и заката.

---

## ⚠️ Важные замечания

1. **Часовой пояс:** Расчет выполняется в локальном времени хаба. Убедитесь, что часовой пояс настроен правильно.

2. **Полярный день/ночь:** Сценарий корректно обрабатывает полярный день и полярную ночь в регионах с высокими широтами. Во время полярного дня выключатель всегда включен (день), во время полярной ночи — всегда отключен (ночь). Сценарий автоматически проверяет окончание полярного периода каждый день в полночь.

---

## 💡 Зачем это нужно?

**Проблема:** Нужно автоматически включать и отключать устройства (например, освещение) в зависимости от времени суток, но время восхода и заката меняется каждый день в течение года.

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание виртуального устройства

Создайте виртуальное устройство типа **"Выключатель"** (Switch) с характеристикой **Включен**.

### Шаг 2: Применение логического сценария

1. Скачайте файл `DayNight.json` из папки сценария
2. Импортируйте сценарий через интерфейс Sprut.Hub:
   - Сценарии → + → Импортировать шаблон → выберите скачанный файл
3. Откройте настройки созданного устройства
4. Перейдите в раздел **"Логика"**
5. Выберите сценарий **"🌅 День-ночь"**
6. Настройте параметры сценария (см. ниже)
7. Активируйте сценарий переключателем "Активно"

**ВАЖНО!** При изменении любого параметра после активации сценария перезапустите сценарий переключателем "Активно" сверху.

### Шаг 3: Настройка параметров

#### Параметр "Широта"
Географическая широта в десятичных градусах. Например:
- **55.7558** — Москва
- **59.9343** — Санкт-Петербург

Диапазон значений: от -90 до 90 градусов.

**Важно:** Если не указано конкретное время включения/отключения, координаты обязательны для расчета времени восхода и заката.

**Где взять координаты:** Текущие координаты можно взять в настройках хаба в разделе **"Дата и время"**, если указали их. Сценарий не имеет доступа к этим сведениям, поэтому необходимо ввести повторно.

#### Параметр "Долгота"
Географическая долгота в десятичных градусах. Например:
- **37.6173** — Москва
- **30.3351** — Санкт-Петербург

Диапазон значений: от -180 до 180 градусов.

**Важно:** Если не указано фиксированное время включения/отключения, координаты обязательны для расчета времени восхода и заката.

#### Параметр "Высота над уровнем моря (метры)"
Высота над уровнем моря в метрах. Влияет на точность расчета времени восхода и заката, так как учитывает кривизну Земли и рефракцию атмосферы.

Например:
- **156** — Москва (значение по умолчанию)
- **3** — Санкт-Петербург
- **0** — города на уровне моря

Диапазон значений: от 0 до 8848 метров (высота Эвереста).

**Важно:** Чем выше местность, тем раньше восход и позже закат. Для большинства случаев разница незначительна, но для горных регионов может быть заметной.

#### Параметр "До/после рассвета"
Смещение времени в минутах от времени восхода:
- **0** — точно во время восхода (по умолчанию)
- **60** — через час после восхода
- **-60** — за час до восхода
- **-30** — за полчаса до восхода

Диапазон значений: от -1440 до 1440 минут (от -24 до +24 часов).

#### Параметр "До/после заката"
Смещение времени в минутах от времени заката (значение аналогично "До/после рассвета")

Диапазон значений: от -1440 до 1440 минут (от -24 до +24 часов).

#### Параметр "Фиксированное время включения"
Фиксированное время включения выключателя. Если указано, это время будет использоваться вместо времени восхода, иначе оставьте пустым.

Формат: **ЧЧ:ММ** в 24-часовом формате. Например: **06:00** — включение в 6 утра

#### Параметр "Фиксированное время отключения"
Фиксированное время отключения выключателя. Если указано, это время будет использоваться вместо времени заката, иначе оставьте пустым.

Формат: **ЧЧ:ММ** в 24-часовом формате. Например: **22:00** — отключение в 10 вечера

#### Параметр "Менять имя сервиса"
Если включено, имя сервиса будет автоматически меняться в зависимости от текущего состояния на **"День"** или **"Ночь"**

#### Параметр "Инвертировать"
Меняет состояние выключателя на обратное:
- **Отключается** во время восхода/включения
- **Включается** во время заката/выключения

Полезно для устройств, которые должны работать ночью (например, ночное освещение).

---

## 🔧 Примеры использования

### Пример 1: Освещение по времени восхода и заката
- **Широта:** 55.7558 (Москва)
- **Долгота:** 37.6173 (Москва)
- **Высота над уровнем моря:** 156 (Москва)
- **До/после рассвета:** 0
- **До/после заката:** 0
- **Фиксированное время включения:** (пусто)
- **Фиксированное время отключения:** (пусто)
- **Менять имя сервиса:** Нет
- **Инвертировать:** Нет

Выключатель будет включаться точно во время восхода и отключаться точно во время заката.

### Пример 2: Освещение за час до заката до часа после восхода
- **Широта:** 55.7558
- **Долгота:** 37.6173
- **Высота над уровнем моря:** 156
- **До/после рассвета:** 60 (через час после восхода)
- **До/после заката:** -60 (за час до заката)
- **Фиксированное время включения:** (пусто)
- **Фиксированное время отключения:** (пусто)
- **Менять имя сервиса:** Нет
- **Инвертировать:** Нет

Выключатель будет включаться за час до заката и отключаться через час после восхода.

### Пример 3: Освещение по фиксированному времени
- **Широта:** (любое значение, не используется)
- **Долгота:** (любое значение, не используется)
- **До/после рассвета:** (не используется)
- **До/после заката:** (не используется)
- **Фиксированное время включения:** 18:00
- **Фиксированное время отключения:** 06:00
- **Инвертировать:** Нет

Выключатель будет включаться в 18:00 и отключаться в 06:00 каждый день.

### Пример 4: Ночное освещение (инверсия)
- **Широта:** 55.7558
- **Долгота:** 37.6173
- **Высота над уровнем моря:** 156
- **До/после рассвета:** 0
- **До/после заката:** 0
- **Фиксированное время включения:** (пусто)
- **Фиксированное время отключения:** (пусто)
- **Менять имя сервиса:** Нет
- **Инвертировать:** Да

Выключатель будет отключаться во время восхода и включаться во время заката (работает ночью).

### Пример 5: С автоматическим изменением имени сервиса
- **Широта:** 55.7558
- **Долгота:** 37.6173
- **Высота над уровнем моря:** 156
- **До/после рассвета:** 0
- **До/после заката:** 0
- **Фиксированное время включения:** (пусто)
- **Фиксированное время отключения:** (пусто)
- **Менять имя сервиса:** Да
- **Инвертировать:** Нет

Выключатель будет работать как в примере 1, но имя сервиса будет автоматически меняться на "День" или "Ночь" в зависимости от текущего состояния.

---

## 🐛 Отладка

Для включения отладочного логирования установите в коде у константы DEBUG значение true вместо false:
```javascript
const DEBUG = true;
```

В логах будет выводиться:
- Время включения и отключения
- Текущее состояние выключателя
- Информация о настроенных cron задачах
- Информация о полярном дне/ночи (если применимо)

